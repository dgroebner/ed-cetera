info:
  name: last event
  type: http
  seq: 2

http:
  method: GET
  url: "{{serverUrl}}/api/status"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
