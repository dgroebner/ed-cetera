info:
  name: last location event
  type: http
  seq: 4

http:
  method: GET
  url: "{{serverUrl}}/api/last_location"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
