info:
  name: all events since
  type: http
  seq: 5

http:
  method: GET
  url: "{{serverUrl}}/api/events/since/:eventId"
  params:
    - name: eventId
      value: "3300"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
