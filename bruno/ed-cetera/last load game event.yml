info:
  name: last load game event
  type: http
  seq: 3

http:
  method: GET
  url: "{{serverUrl}}/api/last_loadgame"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
